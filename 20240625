Bilibili BV1dr4y1n7vA 03:18 P2 
这是我对于为什么需要给计算机下发这么繁杂的指令的解释：
从历史上来看，计算机语言是以基层的汇编语言为前提的
从功能上来看，虽然指令多，但是每条指令很细小且很简单，
组成的结果会使得容错率更高，更不容易出错或漏东西（在编写指令正确且完备的情况下）且会对性能有优化作用——见下面与大指令的对此。
但是如果咱们用的像是一条大指令但是其内容包括好几条小指令，但如果我们编写一个程序，这个大指令并不全面，
我们就需要去引入其他拥有补全项的大指令，
这其中可能引入许多冗杂指令，所以可能会危害性能
